package com.plantilla.backend.shared.enums;

/**
 * Niveles del semáforo de ocupación (almacenes y vuelos).
 * Determinado por umbrales parametrizables (RAL-04).
 */
public enum NivelSemaforo {
    VERDE,
    AMBAR,
    ROJO
}
